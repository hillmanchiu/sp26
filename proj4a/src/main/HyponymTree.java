package main;

public class HyponymTree {
    //We want to implement a graph
    //We can implement a graph by implementing data structures:

    // There should be a data structure which includes each node and its children node.
    // We can achieve this by creating a hashmap of each synset and a list or array of its children nodes.

    // There should be a second data structure that consists of each word and its synset.
    //This can be achieved using a hashmap of a String and a list or array.

}


